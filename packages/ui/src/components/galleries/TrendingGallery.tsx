import Thumbnail from "../thumbnail/Thumbnail";

const TrendingGallery = () => {
  return (
    <div>
      {/* {trendingMovies.map((movie) => (
        <Thumbnail isTouch={true} {...movie} />
      ))} */}
    </div>
  );
};

export default TrendingGallery;
